# Empty HTML Project

## Overview
A basic web project with an empty HTML file served by Python's built-in HTTP server.

## Recent Changes
- **November 17, 2025**: Created initial empty index.html file with basic HTML5 structure
- Set up Python HTTP server workflow on port 5000

## Project Structure
```
.
├── index.html          # Empty HTML file with basic structure
└── replit.md          # This file
```

## Running the Project
The project uses a Python HTTP server workflow that automatically starts and serves the HTML file on port 5000.
